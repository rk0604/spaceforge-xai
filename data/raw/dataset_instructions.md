# Dataset Documentation Instructions  
For `data/raw/<RUN_ID>/`

This file defines the **standard format** for documenting any dataset produced by a simulation run.  
Each run creates a directory:

```
data/raw/<RUN_ID>/
```

Inside that directory are several CSV files generated by either SPARTA, the C++ harness, or post‑processing tools.  
To keep all runs reproducible and self‑describing, **every run directory must contain a `run_metadata.md` file** following the template below.

---

## 🔧 How to Use This Template

1. **Copy** everything between `TEMPLATE START` and `TEMPLATE END`.  
2. **Paste** into:  
   ```
   data/raw/<RUN_ID>/run_metadata.md
   ```  
3. Fill in everything inside `<angle brackets>`.  
4. Do **not** leave blank fields — use `UNKNOWN` if uncertain.  
5. Keep one metadata file per run. Never mix runs.

---

# TEMPLATE START

# Run Metadata — `<RUN_ID>`

**Run ID:** `<RUN_ID>`  
**Directory:** `data/raw/<RUN_ID>/`  
**Local Time:** `<YYYY-MM-DD HH:MM>`  
**UTC Time:** `<YYYY-MM-DD HH:MM>`  

---

## 1. Simulation Configuration (High-Level)

| Setting | Value |
|--------|-------|
| Simulation mode (`--mode`) | `<wake | power | legacy | other>` |
| ENABLE_SPARTA | `<ON | OFF>` |
| GPU mode (`GPU`) | `<ON | OFF>` |
| MPI ranks (`NP`) | `<e.g., 1, 4, 8>` |
| Wake deck (`WAKE_DECK`) | `<in.wake / in.wake_harness / ...>` |
| Input subdir | `<path>` |

**Purpose of this run (1–3 sentences):**  
`<Describe what this run tests or explores.>`

---

## 2. Orbit & Environment Parameters (passed to SPARTA via -var)

| Variable | Meaning | Value |
|----------|----------|--------|
| `PTORR_TARGET` | Freestream ambient pressure [Torr] | `<1e-7>` |
| `PCUP_TORR` | Added wake pressure at cupola [Torr] | `<9e-9>` |
| `FWAfer_CM2S` | Wafer-center flux [cm⁻² s⁻¹] | `<1e14>` |
| `CUP_BASE_SCALE` | Base outgassing scale | `<1.0>` |
| `CUP_AMP_SCALE` | Outgassing sinusoidal amplitude | `<0.5>` |
| `CUP_PHASE0` | Phase offset | `<0.0>` |

**Additional physical parameters:**  
- Altitude: `<e.g., 400 km>`  
- Temperature: `<e.g., 800 K>`  
- Velocity: `<e.g., 7500 m/s>`  
- Notes: `<...>`

---

## 3. Harness Timing / Coupling Configuration

| Parameter | Description | Value |
|----------|-------------|--------|
| `nticks` | Total harness ticks | `<500>` |
| `dt` | Seconds of physical time per tick | `<0.1>` |
| `sparta_block` | Ticks between SPARTA calls | `<200>` |
| `couple_every` | Frequency of C++↔SPARTA coupling | `<10>` |

**Extra CLI flags used:**  
```
<e.g., --nticks 500 --sparta-block 200>
```

---

## 4. Code + Deck Versions (Reproducibility)

| Item | Value |
|------|--------|
| Git repo | `<spaceforge-xai>` |
| Branch | `<main / dev / ...>` |
| Commit hash | `<abcdef123>` |
| Uncommitted changes? | `<yes/no + short description>` |
| SPARTA build used | `<GPU build-gpu / CPU src>` |
| SPARTA executable | `<full path>` |
| Important SPARTA flags | `<-k on g 1 -sf kk>` |

---

## 5. Files in This Dataset

For each file in `data/raw/<RUN_ID>/`, document:

### `<filename_1.csv>`
- **Source:** `<SPARTA | C++ harness | post-processing>`  
- **Description:** `<one sentence>`  
- **One row represents:** `<tick / minute / timestep / sample>`  
- **Key columns:**
  - `column_name [units]` — `<meaning>`
  - …

### `<filename_2.csv>`
*(Repeat same structure as above)*

### `<filename_3.csv>`
*(As needed)*

---

## 6. Observations, Anomalies, and Notes

- Runtime behavior: `<normal / warnings / convergence notes>`  
- SPARTA warnings/errors: `<if any>`  
- Harness anomalies: `<NaNs, divergence, early termination>`  
- Transient behaviors to ignore in analysis: `<e.g., drop first 10 rows>`  
- Why this run is unique: `<parameter sweep / GPU tuning / new deck>`  

---

# TEMPLATE END