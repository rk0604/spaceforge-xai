Here’s all **8 realism improvements** summarized compactly:

1. **Diurnal Variation:**
   Use continuous sinusoidal outgassing (1×–10×) with ¼-orbit phase lag, not step-wise toggles.

2. **Outgassing Rate Magnitude:**
   Base flux on measured WSF rates (10⁹–10¹³ cm⁻² s⁻¹); optionally decay with orbit (bake-out).

3. **Thermal Coupling:**
   Add temperature lag between shield and chassis; use separate `Tsurf` variables per region.

4. **Gauge Outgassing:**
   Add internal gauge offset term (P_{meas}=P_{true}+αe^{β(T-300)}) to model sensor heating effects.

5. **Wake Geometry & Reflections:**
   Make cupola surfaces partly diffuse (`diffuse 0.1`) and add a small re-emission source near the nozzle.

6. **Orbit & Environment:**
   Vary ambient pressure sinusoidally (~2× day/night) and modulate `vstream` 7500 m/s ± 5 %.

7. **Long-Term Conditioning:**
   Introduce orbit-to-orbit exponential decay `bake_factor=exp(-io/10)` to simulate surface cleanup.

8. **Data Cadence:**
   Record every 15–30 s (≈ 188–376 rows/orbit) for smoother thermal waveform, then downsample to 1 min if needed.
